# MeshCentral Portable public entry point

This repository contains only a small redirect page. It does not contain the
MeshCentral database, certificates, credentials, agents, or `node_modules`.

The local launcher updates the redirect target whenever the public tunnel URL
changes.
